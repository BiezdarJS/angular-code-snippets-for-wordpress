import { HandleLinkClickDirective } from './handle-link-click.directive';

describe('HandleLinkClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleLinkClickDirective();
    expect(directive).toBeTruthy();
  });
});
